User Interface components including dashboard views presenting alert summary, severity visualization, explanation panels, recommended actions, and filters. Provides real-time updates, historical alert navigation, and quick-action controls for network operators.

**Recent updates (2026-07-16):**
- Three-state Watson AI card in AlertDetailsPage: `InlineLoading` spinner + `SkeletonText` while re-analyzing, empty state with Watson icon when no analysis exists, full content when ready
- Security response headers via `vercel.json`: Content-Security-Policy, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy
- Design token unification: all page content gaps use `$tok-page-content-gap`; stale `--page-*` CSS vars removed
- Input character limits with Carbon counters: ticket comments (2000), title (256), description (5000), resolve notes (2000)

### UML Diagrams in this folder:

